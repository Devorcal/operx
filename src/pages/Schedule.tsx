import { FunctionComponent } from "react";
import styles from "./Schedule.module.css";

const Schedule: FunctionComponentScheduleType = () => {
  return <div className={styles.schedule} />;
};

export default Schedule;
